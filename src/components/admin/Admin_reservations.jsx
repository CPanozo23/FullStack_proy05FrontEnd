const Admin_reservations = () => {

    return(
        <section>
            <p>- Reservas (Próximas [op:cancelar, cambiar fecha/hora], anteriores)</p>
        </section>
    )
}

export default Admin_reservations